# Shared (optional)
# NOTE: Each app also has its own .env.example.

# --- Backend ---
# These map directly to `app.core.settings.Settings`.
DATABASE_URL=postgresql+asyncpg://edupredict:edupredict@localhost:5432/edupredict
JWT_SECRET=REPLACE_ME_WITH_A_LONG_RANDOM_SECRET
JWT_ISSUER=edupredict
JWT_AUDIENCE=edupredict-web
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=14

# Admin bootstrap (disabled by default)
ALLOW_ADMIN_BOOTSTRAP=false
ADMIN_BOOTSTRAP_TOKEN=

# CORS
CORS_ALLOW_ORIGINS=http://localhost:3000

# --- Frontend ---
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
